[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "22"

# Official Next.js runtime for Netlify — handles SSR, API routes, and the
# App Router automatically. Netlify installs this plugin on detection.
[[plugins]]
  package = "@netlify/plugin-nextjs"
